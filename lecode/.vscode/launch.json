{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run SDL2 Game",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/game",
            "cwd": "${workspaceFolder}",
            "MIMode": "lldb",
            "preLaunchTask": "Build SDL2 Project"
        }
    ]
}
